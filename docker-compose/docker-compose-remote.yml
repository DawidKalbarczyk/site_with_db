services:
  db:
    build:
      context: ..
      dockerfile: dockerfile/postgres/Dockerfile
    image: postgres-db
    container_name: postgres-db-${ENV-NAME}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - ${POSTGRES_PORT}:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data



  web:
    build:
      context: ..
      dockerfile: dockerfile/web/Dockerfile
    image: site-web
    container_name: site-web-${ENV-NAME}
    environment:
      WEB_TEST: ${WEB_TEST}
    ports:
      - ${WEB_PORT}:80
    depends_on:
      - db

volumes: 
  postgres_data: